source 'https://rubygems.org'
ruby '2.1.2'

#rails always has to be the first
gem 'rails', '4.1.1'

# Application Gems
gem 'bootstrap-sass'
gem 'carrierwave'
gem 'coffee-rails', '~> 4.0.0'
gem 'font-awesome-sass'
gem 'jbuilder', '~> 2.0'
gem 'jquery-rails'
gem 'mini_magick'
gem 'pg'
gem 'sass-rails', '~> 4.0.3'
gem 'thin'
gem 'turbolinks'
gem 'uglifier', '>= 1.3.0'

# Development Gems
group :development do
  gem 'pry-rails'
  gem 'spring' #for preloading ur app to speed up development
end

group :doc do
  gem 'sdoc', '~> 0.4.0',          group: :doc
end

# test gems
group :test do

end

#production gems
group :production do
  gem 'rails_12factor' #this is only for heroku
end
